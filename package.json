{
  "name": "pricing-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "build:cards": "turbo run build --filter cards-widget",
    "dev:main": "turbo run dev --filter main",
    "dev:cards": "turbo run dev --filter cards-widget",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "engines": {
    "node": ">=16.8.0"
  },
  "dependencies": {},
  "packageManager": "npm@8.19.3"
}
